{
  "name": "cardinality-estimator",
  "version": "1.0.0",
  "description": "estimates the cardinality of basic queries on a single table using histogram-based statistics.",
  "main": "index.ts",
  "repository": "git@github.com:leibatt/cardinality-estimator.git",
  "author": "Leilani Battle <leibatt@cs.washington.edu>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest --collectCoverage=true && yarn lint",
    "lint": "eslint ."
  },
  "dependencies": {
    "eslint": "^8.34.0",
    "eslint-plugin-jest": "^27.2.1",
    "jest": "^29.4.3",
    "node-sass": "^8.0.0",
    "typescript": "^4.9.5"
  }
}
